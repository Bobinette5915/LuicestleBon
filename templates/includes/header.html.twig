<!-- ======= Header ======= -->
<header>

	<nav class="navbar navbar-expand-lg" id='navbar'>
		<div class="container-fluid">
			<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarTogglerDemo01" aria-controls="navbarTogglerDemo01" aria-expanded="false" aria-label="Toggle navigation">
				<span class="navbar-toggler-icon"></span>
			</button>
			<a href="{{ path('app_accueil') }}">
				<img id='logonav' src="{{ asset('assets/images/logo.svg') }}" alt="Logo" width="150px">
			</a>
			<div class="collapse navbar-collapse" id="navbarTogglerDemo01">

				<ul class="navbar-nav me-auto mb-2 mb-lg-0">
					<li class="nav-item">
						<h5>
							<a class="nav-link " href="{{ path('app_boxs') }}">Nos Box's</a>
						</h5>
					</li>
					<li class="nav-item">
						<h5>
							<a class="nav-link " href="{{ path('app_partenaires') }}">Nos Partenaires</a>
						</h5>
					</li>
					<li class="nav-item">
						<h5>
							<a class="nav-link " href="{{ path('app_contact') }}">Une Question ?</a>
						</h5>
					</li>
					
						{% if app.user %}
						<li class="nav-item">
							<a class="nav-link " href="{{ path('app_compte') }}">
								<h5>Mon compte</h5>
							</a>
						</li>
					{% else %}
						<li class="nav-item">
							<a class="nav-link " href="{{ path('app_connexion') }}">
								<h5>Se connecter</h5>
							</a>
						</li>
					{% endif %}
				</ul>
				{% if app.user %}
					<a class="nav-link " href="{{ path('app_logout') }}">
						<img src="{{ asset('assets/images/logout.png') }}" width="20px" alt="se-deconnecter" id='imglogout'>
					</a>
					<a class="nav-link " href="{{ path('app_cart') }}">
						<img src="{{ asset('assets/images/paniers.png') }}" width="20px" alt="panier">
					</a>
				{% endif %}
			</div>
		</div>
	</nav>
</header>
